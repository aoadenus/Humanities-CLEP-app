{
  "name": "private-clep-humanities-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start",
    "lint": "npm --prefix apps/web run lint",
    "typecheck": "npm --prefix apps/web run typecheck",
    "check:content": "npm --prefix apps/web run check:content",
    "release:check": "npm --prefix apps/web run release:check",
    "publish:content": "npm --prefix apps/web run publish:content",
    "verify:hosted": "npm --prefix apps/web run verify:hosted"
  }
}
